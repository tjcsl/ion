window.addEventListener("load", function() {
    Messenger().info({
        message: "Why are the Ion devs so bad? I can't find my 8th periods.",
        hideAfter: 10,
        showCloseButton: true,
    });
});